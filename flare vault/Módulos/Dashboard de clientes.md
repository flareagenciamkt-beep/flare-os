---
tags: [modulo]
fuente: app/(app)/clients/dashboard/page.tsx
actualizado: 2026-07-15
---

# Dashboard de clientes

Landing por defecto de la app (`/` redirige aquí). Resumen de la cartera de [[Client|clientes]]: estados ([[ClientStatus]]), salud ([[HealthStatus]]) y resúmenes por cliente (`summarizeClient` en `lib/stats.ts`).

La card "Progreso promedio" (`averageProgress`, ver [[Progreso operativo]]) enlaza a `/clients` desde `92cf0ab` (antes apuntaba a la vista global `clients/progress`, retirada).

Ruta: `app/(app)/clients/dashboard/page.tsx`.
